<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="iAudit.Views.AddIncomePage"
             Title="Add Income">
    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Cancel" Clicked="Cancel_Clicked" />
        <ToolbarItem Text="Add" Clicked="Save_Clicked" />
    </ContentPage.ToolbarItems>
    <ContentPage.Content>
        <StackLayout Spacing="20" Padding="15">
            <Label Text="Income Title" FontSize="Medium" />
            <Entry Text="{Binding Income.IncomeName}" d:Text="Income Title" FontSize="Small" />
            <Label Text="Notes" FontSize="Medium" />
            <Editor Text="{Binding Income.Notes}" d:Text="Income notes" FontSize="Small" Margin="0" />
            <Label Text="Amount" FontSize="Medium" />
            <Editor Text="{Binding Income.Amount}" d:Text="Income Amount" FontSize="Small" Margin="0" />
            <Label Text="Date" FontSize="Medium" />
            <Editor Text="{Binding Income.Date}" d:Text="Date MM-DD-YYYY" FontSize="Small" Margin="0" />
        </StackLayout>
    </ContentPage.Content>
</ContentPage>

