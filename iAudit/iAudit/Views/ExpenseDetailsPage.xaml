<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             Title="{Binding Title}"
             x:Class="iAudit.Views.ExpenseDetailsPage">
    <ContentPage.Content>
        <StackLayout Spacing="20" Padding="15">
            <Label Text="Income Name:" FontSize="Medium" />
            <Label Text="{Binding Expense.ExpenseName}" d:Text="Expense name" FontSize="Small" TextColor="DarkRed"/>
            <Label Text="Amount:" FontSize="Medium" />
            <Label Text="{Binding Expense.Amount}" d:Text="Expense amount" FontSize="Small" TextColor="DarkRed"/>
            <Label Text="Notes:" FontSize="Medium" />
            <Label Text="{Binding Expense.Notes}" d:Text="Expense notes" FontSize="Small" TextColor="DarkRed"/>
            <Label Text="Date:" FontSize="Medium" />
            <Label Text="{Binding Expense.Date}" d:Text="Expense date" FontSize="Small" TextColor="DarkRed"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>
