<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="iAudit.Views.LoginPage"
             Title="{Binding Title}">
    <ContentPage.Content>
        <StackLayout Orientation="Vertical" Margin="40" Padding="40" VerticalOptions="Center" HorizontalOptions="Center">
             <StackLayout Spacing="20" Padding="15" Orientation="Vertical">
                <Image Source="@drawable/logo.png" WidthRequest="100" HeightRequest="100"/>
                <Entry Placeholder="enter email" />
                <Entry Placeholder="enter password" IsPassword="True" />
                <StackLayout Spacing="20" Padding="15" Orientation="Horizontal">
                    <Button Text="LOGIN" Clicked="LOGIN_Clicked"/>
                    <Button Text="REGISTER" Clicked="REGISTER_Clicked"/>
                </StackLayout>
        </StackLayout>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>
